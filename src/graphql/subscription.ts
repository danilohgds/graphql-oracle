/* import { PubSub } from 'graphql-subscriptions';
import { appGraphQLSchema } from './schema';

export const pubsub = new PubSub();

export const subscription = new SubscriptionManager({
    schema: appGraphQLSchema,
    pubsub,
    setupFunctions: {},
});

 */